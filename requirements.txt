# Runtime dependencies for qtt_dlrx_simple (pip-installable, platform-neutral).
# For a reproducible conda environment, use environment.yml instead.
#
# quimb is pinned to 1.4.0: later releases change the tensor-network API used
# throughout this codebase.
quimb==1.4.0
numpy
scipy
matplotlib
findiff
sympy
# Test runner for the installation-verification suite (tests/test_install_xfunc.py).
pytest
